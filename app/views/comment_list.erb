
<% @comments.each do |c| %>
  <div class="hero-unit">
    <p><b>Created on <%= c.created_at.strftime('%B %d, %Y') %> by <%= c.user.email %></b> | <a href= "/comment/<%= c.id %>">link</a> | <a href="/post/<%= c.post.id %>">parent</a></p>
    <p><%= c.content %></p>
  </div>
<% end  %>
